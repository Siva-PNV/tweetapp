version: 0.2

phases:
  pre_build:
    commands:
      - echo Installing source NPM dependencies...
      - mvn install
  post_build:
    commands:
      - echo completed
artifacts:
  files:
    - target/TweetApp.jar
